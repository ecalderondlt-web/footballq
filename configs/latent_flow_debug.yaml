experiment: latent_flow_debug
seed: 123
data:
  rollout_dataset: data/processed/skillcorner_latent_rollout_dataset.pt
model:
  name: latent_flow_mlp
  latent_dim: 128
  context_steps: 5
  horizon_steps: 5
  hidden_dim: 64
  num_layers: 2
  dropout: 0.0
  time_embed_dim: 32
training:
  batch_size: 128
  max_epochs: 1
  max_train_batches: 5
  learning_rate: 0.0005
  weight_decay: 0.00001
  seed: 123
  grad_clip_norm: 1.0
  num_workers: 0
  device: cpu
  run_root: runs
  best_metric: loss
sampling:
  num_samples: 2
  num_steps: 4
